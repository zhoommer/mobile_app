import { StyleSheet } from "react-native";

export const errorMessage = StyleSheet.create({
  error: {
    position: "absolute",
    bottom: -25,
    left: 20,
    color: "#1b1e3d",
    fontSize: 14,
    fontWeight: 800,
  },
});
