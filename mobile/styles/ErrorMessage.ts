import { StyleSheet } from "react-native";

export const errorMessage = StyleSheet.create({
  error: {
    position: "absolute",
    bottom: -25,
    left: 20,
    color: "#dc2626",
    fontSize: 14,
    fontWeight: 600,
  },
});
