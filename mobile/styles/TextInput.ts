import { StyleSheet } from "react-native";

export const textInputStyle = StyleSheet.create({
  input: {
    backgroundColor: "#fff",
    color: "#000",
    borderRadius: 10,
    padding: 10,
    paddingLeft: 60,
  },
});
